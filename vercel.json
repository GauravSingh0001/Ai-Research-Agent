{
    "version": 2,
    "buildCommand": "pip install -r requirements.txt",
    "builds": [
        {
            "src": "api/index.py",
            "use": "@vercel/python@3.1.0",
            "config": {
                "maxLambdaSize": "250mb"
            }
        }
    ],
    "routes": [
        {
            "src": "/(.*)",
            "dest": "api/index.py"
        }
    ],
    "env": {
        "PYTHONPATH": "./",
        "VERCEL": "1"
    }
}